<% if !@product.taxons.blank? -%>
<div class="relatedposts">
	<h3><%= t('look_for_similar_items') -%></h3>
	<ul>
		<% @product.taxons.each do |taxon| -%>
		<li>
			<a href="<%="/t/#{taxon.permalink}"%>"><img style="border: none;" alt="" src="<%=taxon.products.active.first.images.first.attachment.url(:mini)%>" /></a>
			<%= link_to taxon.name, "/t/#{taxon.permalink}", :rel => "bookmark", :title => taxon.name %>
		</li>
		<% end %>
	</ul>
</div>
<% end -%>